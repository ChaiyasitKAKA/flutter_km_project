import 'package:flutter/material.dart';

class Home2UI extends StatelessWidget {
  const Home2UI({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
